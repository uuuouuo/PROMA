server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/proma1?serverTimezone=UTC&characterEncoding=UTF-8
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

  properties:
    hibernate:
      format_sql: true

  security:
    oauth2:
      client:
        registration:
          github:
            clientId: e9aef6fccada43586c11
            clientSecret: 9ad91b0d27056363f971845f3bee0dc46f93d472

logging.level:
  org.hibernate.SQL: debug